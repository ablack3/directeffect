% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pool-meta.R
\name{pool_meta}
\alias{pool_meta}
\title{Pool comparative estimates by inverse-variance meta-analysis}
\usage{
pool_meta(
  de,
  method = c("fixed", "random"),
  balance = NULL,
  max_smd = 0.1,
  min_balanced_frac = 0.1
)
}
\arguments{
\item{de}{A \code{directeffect_network}, or a data frame with \code{target},
\code{comparator}, \code{estimate} and \code{std_error}.}

\item{method}{\code{"fixed"} for a common-effect inverse-variance pool, or
\code{"random"} for DerSimonian--Laird, which adds a between-comparison variance
\code{tau^2} estimated by the method of moments.}

\item{balance, max_smd, min_balanced_frac}{Optional balance filter, applied
exactly as in \code{\link[=pool_bootstrap]{pool_bootstrap()}}, so the two can be compared on identical
subsets.}
}
\value{
A data frame with one row per drug appearing as a target: \code{drug},
\code{n_trials}, \code{n_balanced}, \code{frac_balanced}, \code{pooled_hr}, \code{ci_lower},
\code{ci_upper}, \code{log_hr}, \code{se_log_hr}, \code{p_value} (two-sided), \code{tau2},
\code{i_squared}, \code{q_p_value} (Cochran's Q test of homogeneity), \code{method},
\code{reported}.
}
\description{
Combines a drug's comparative estimates by weighting each by its precision,
the standard fixed-effect or random-effects (DerSimonian--Laird) meta-analysis.
}
\details{
This is the efficient version of what \code{\link[=pool_bootstrap]{pool_bootstrap()}} does crudely. Both
average a drug's estimates as they stand; meta-analysis weights each by
\code{1/se^2} instead of equally, and its interval comes from the pooled variance
rather than from resampling trials.

\strong{The two share an estimand, and therefore share a bias.} Both answer "how
did this drug fare against the comparators it happened to be tested against?"
Neither can distinguish a protective drug from one that was simply compared
against harmful drugs, because a comparator's own effect never enters the
calculation. Weighting fixes efficiency; it does not fix that. Only solving
the network (\code{\link[=fit_surface]{fit_surface()}}) estimates the comparators' positions rather
than assuming them to be zero.
}
\examples{
de <- direct_effect_network(example_comparisons, effect_measure = "HR")
pool_meta(de, method = "random")
}
\seealso{
\code{\link[=pool_bootstrap]{pool_bootstrap()}}, \code{\link[=fit_surface]{fit_surface()}}, \code{\link[=compare_pooling]{compare_pooling()}}.
}
