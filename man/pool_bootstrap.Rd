% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pool-bootstrap.R
\name{pool_bootstrap}
\alias{pool_bootstrap}
\title{Pool comparative estimates by bootstrapped mean across trials}
\usage{
pool_bootstrap(
  de,
  balance = NULL,
  max_smd = 0.1,
  min_balanced_frac = 0.1,
  R = 1000,
  scale = c("hr", "log"),
  seed = NULL
)
}
\arguments{
\item{de}{A \code{directeffect_network} from \code{\link[=direct_effect_network]{direct_effect_network()}}, or a plain
data frame of comparisons with \code{target}, \code{comparator} and \code{estimate}.}

\item{balance}{Optional data frame with \code{target}, \code{comparator} and
\code{max_abs_smd}, used to keep only balanced trials. The source paper counts a
trial as balanced when at most 2\% of covariates exceed |SMD| 0.1; supply
whatever balance summary the upstream pipeline produced and set
\code{max_smd} to the threshold.}

\item{max_smd}{Balance threshold applied to \code{balance$max_abs_smd}. Trials
above it are dropped. Ignored when \code{balance} is \code{NULL}.}

\item{min_balanced_frac}{A drug is reported only if at least this fraction of
its trials survive the balance filter. The source paper uses 0.10. Ignored
when \code{balance} is \code{NULL}.}

\item{R}{Bootstrap resamples. Default 1000, as in the source paper.}

\item{scale}{\code{"hr"} averages on the hazard-ratio (linear) scale, which is
what the source paper does. \code{"log"} averages log hazard ratios and
exponentiates, which is the scale the estimates are actually symmetric on
and is usually the better choice; it is not what the paper did.}

\item{seed}{Optional integer seed. The caller's RNG state is restored on
exit, so pooling never disturbs the session's reproducibility.}
}
\value{
A data frame with one row per drug that appears as a target:
\code{drug}, \code{n_trials}, \code{n_balanced}, \code{frac_balanced}, \code{pooled_hr},
\code{ci_lower}, \code{ci_upper}, \code{p_value} (bootstrap, one-sided against
HR < 1, as in the source paper), \code{scale}, and \code{reported} (whether it
cleared \code{min_balanced_frac}).
}
\description{
Averages a drug's comparative estimates directly, with a non-parametric
bootstrap over the estimates for the interval. This is the pooling used in
high-throughput target-trial emulation (Zang et al., \emph{Nature Communications}
2023;14:8180): per drug, take the sample mean of the adjusted hazard ratios
over the trials that balanced, bootstrap the trials 1,000 times for a 95\%
interval, and test whether that mean is below 1.
}
\details{
It is offered here as a deliberate contrast to \code{\link[=fit_surface]{fit_surface()}}, not as an
alternative implementation of it. The two answer different questions:
\itemize{
\item \code{pool_bootstrap()} asks \strong{"how did this drug fare against the comparators it
happened to be tested against?"} Each estimate enters as-is, so the answer
inherits whatever the comparators' own effects were. A drug compared mostly
against harmful drugs looks protective, and nothing in the method can tell
that apart from the drug being protective.
\item \code{\link[=fit_surface]{fit_surface()}} asks \strong{"where does this drug sit relative to every other
drug?"}, solving the whole network at once so a comparator's own position
is estimated rather than assumed to be neutral.
}

Where the two disagree, the disagreement is informative: it measures how far
a drug's comparator set sits from the network average; \code{\link[=compare_pooling]{compare_pooling()}}
reports that quantity per drug.
}
\examples{
de <- direct_effect_network(example_comparisons, effect_measure = "HR")
pool_bootstrap(de, R = 200, seed = 1)
}
\references{
Zang C, Zhang H, Xu J, et al. High-throughput target trial emulation for
Alzheimer's disease drug repurposing with real-world data.
\emph{Nature Communications} 2023;14:8180.
}
\seealso{
\code{\link[=fit_surface]{fit_surface()}} for the network alternative, and
\code{\link[=compare_pooling]{compare_pooling()}} to run both on one network and quantify the gap.
}
