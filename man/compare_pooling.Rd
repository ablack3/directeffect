% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pool-bootstrap.R
\name{compare_pooling}
\alias{compare_pooling}
\title{Compare bootstrapped pooling with the fitted surface}
\usage{
compare_pooling(de, fit = NULL, ...)
}
\arguments{
\item{de}{A \code{directeffect_network}.}

\item{fit}{A \code{directeffect_fit} from \code{\link[=fit_surface]{fit_surface()}} on the same network. If
\code{NULL}, one is fitted with the \code{netmeta} engine.}

\item{...}{Passed to \code{\link[=pool_bootstrap]{pool_bootstrap()}}.}
}
\value{
A data frame with one row per target drug: the pooled estimate, the
surface estimate, \code{comparator_offset}, and the difference in log HR.
}
\description{
Runs \code{\link[=pool_bootstrap]{pool_bootstrap()}} and lines its per-drug answer up against a
\code{\link[=fit_surface]{fit_surface()}} fit of the same network, on the same reference scale.
}
\details{
The two are put on a common footing by re-expressing the surface relative to
each drug's own comparators: a drug's surface position minus the
evidence-weighted mean position of the drugs it was compared against. That
difference is what pooling implicitly assumes is zero -- it treats every
comparator as if it sat at the network average. \code{comparator_offset} reports
how wrong that assumption is for each drug, and is the quantity that explains
where the two methods part company.
}
